/*

Functions needed

fit(X,y,alpha,i){
    creates theta matrix
    theta, cost = ETL.gradiantdescent(X,y,theta,alpha,i)
    return theta
}

pred(theta,X_test){
    return (theta[0] + sum(theta[1:]*X_test))
}

*/
